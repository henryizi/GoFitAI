// Simple test to check mock store
const path = require('path');

// Try to load the mock data (will fail due to TypeScript syntax)
try {
  console.log('Mock store test - checking if metabolic calculations are working');
} catch (error) {
  console.log('Error loading mock store:', error.message);
}





