[phases.setup]
nixPkgs = ['nodejs', 'vips', 'pkg-config']

[phases.install]
cmds = ['npm install', 'cd server && npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node server/index.js'

